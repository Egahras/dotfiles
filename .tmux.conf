# forece tmux to use 256 color in term
set -g default-terminal "screen-256color"

# ColorShemePlugin
set -g @plugin 'jimeh/tmux-themepack'
set -g @themepack 'powerline/default/gray'

# TmuxPluginManager
run -b '~/.tmux/plugins/tpm/tpm'
